<li>
    {{name}}

    <button class="change-burgers" data-id="{{id}}" data-newsleep="{{sleep}}">
        {{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
    </button>

    <button class="delete-burgers" data-id="{{id}}">DELETE!</button>
</li>